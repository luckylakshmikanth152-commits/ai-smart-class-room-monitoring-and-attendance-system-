# =============================================================
# Smart Classroom — Python 3.10 install instructions
# =============================================================
# DO NOT just run `pip install -r requirements.txt` first.
# Install in this order to avoid dlib build errors:
#
#   pip install --upgrade pip setuptools wheel
#   pip install numpy==1.24.3
#   pip install cmake
#   pip install dlib-bin              # Windows (pre-built wheel)
#     -- OR --
#   pip install dlib                  # Linux / Mac (needs build tools)
#   pip install face-recognition --no-deps
#   pip install face-recognition-models
#   pip install -r requirements.txt
# =============================================================

Django==4.2.7
numpy==1.24.3
opencv-python==4.8.1.78
Pillow==10.1.0
ultralytics==8.0.200
APScheduler==3.10.4
django-apscheduler==0.6.2
python-decouple==3.8
